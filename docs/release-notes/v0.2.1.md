# v0.2.1

Compatibility fix for rewrite strength naming.

## Fixes

- Accept `--rewrite-strength high` as an alias for canonical `heavy`.
- Normalize rewrite strength aliases before prompt construction.
- Document that `heavy` is the preferred command value while `high` remains
  accepted for compatibility.

## Validation

- Unit test suite covers the `high` alias at prompt and CLI level.
- Routing eval passes.

